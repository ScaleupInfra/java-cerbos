---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: "your_resource_type" # Replace with your actual resource type
  rules:
    - actions: ["*"] # Applies to all actions
      effect: EFFECT_ALLOW
      roles:
        - "*" # Applies to all roles
